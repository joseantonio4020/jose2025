import { MayusculasPrimeraPipe } from './mayusculas-primera-pipe';

describe('MayusculasPrimeraPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusculasPrimeraPipe();
    expect(pipe).toBeTruthy();
  });
});
